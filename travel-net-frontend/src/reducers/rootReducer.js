const defaultState = {yo: "hi"}

function rootReducer(state = defaultState, action) {
  return state;
}

export default rootReducer;


// minute 12:14 how to set this up but i want to see the combined
// connect on 17:30
